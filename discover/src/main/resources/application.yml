server:
  port: 8761

spring:
  application:
    name: eureka-server

  cloud:
    config:
      enabled: true

  config:
    import: "optional:configserver:http://config-server:8888"

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false